# .dockerignore
# Exclude directories/files that must NOT be sent to the Docker build context.
# Without this, node_modules (~400MB) floods the build context and makes
# `docker compose build` take minutes instead of seconds.

node_modules
dist
.env
.env.*
!.env.example
*.log
coverage
.git
.gitignore
README.md
